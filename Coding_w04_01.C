#include <stdio.h>  // Keyword

int main() {        // Keyword (int), Identifier (main)
    printf("Hello,\nC Tokens!"); // Identifier (printf), Constant ("Hello,\nC Tokens!"), Operator (())
    return 0;        // Keyword (return), Constant (0), Operator (;)
}